import {debugData} from "./utils/debugData";

debugData([
  {
    action: 'setVisible',
    data: true,
  }
])

const App: React.FC = () => {
  return (
    <div>
      Hello
    </div>
  )
}

export default App
